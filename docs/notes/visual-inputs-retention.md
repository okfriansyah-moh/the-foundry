# Visual Inputs Retention Wiring

Mockup ingestion stores source artifacts under `data/visual-inputs/<artifact-id>/` with a sibling `metadata.json` row.
This path is the retention class for visual inputs in Task 43 and is intentionally separate from runtime evidence paths.
