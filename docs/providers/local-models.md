# Local Models Provider Profile

[← Back to Delivery Foundry master index](../../delivery_foundry.md) · [Migration map](../../docs/MIGRATION_MAP_V11_TO_V12.md)

Status: capability stub. Local/self-hosted models (e.g., via Ollama-compatible or OpenAI-compatible endpoints) integrate through the same LLM capability contract: explicit capability declaration, no implicit feature assumptions, capacity signals derived from local hardware saturation, zero-cost ledger entries with shadow pricing optional for comparison.

Security note: local models still receive only data allowed by the profile's classification policy; "local" does not bypass classification rules.
