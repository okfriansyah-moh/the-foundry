// Package inputrouter is the authority-neutral production input router
// (docs/PLAN.md Task 150). It carries authenticated intent and selects an
// application path; admission/approval/kernel remain the only authority owners.
package inputrouter
