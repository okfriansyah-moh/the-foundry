# Seeded fitness violation

Seeded violation for docs/PLAN.md Task 37 / FND-18: `fitlint mermaidid` must
flag the same Mermaid diagram ID reused in more than one heading.

### D-99 — Duplicate diagram ID (first occurrence)

```mermaid
flowchart TB
    A --> B
```
